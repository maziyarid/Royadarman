# Presentation rescue progress

Living log for branch `presentation-rescue-2026-09-16` / draft PR https://github.com/maziyarid/Royadarman/pull/9.

Hourly automation continues this file. Newest entry first.

## 2026-09-16 23:15 EDT — hour 3

Automation continues on `presentation-rescue-2026-09-16`. Never push `main`.

Shipped this hour:

1. **Guided patient request wizard (High gap)**
   - Blade multi-step flow: service → urgency → location → preferences → details → documents note → consent → review/submit.
   - Fail-closed when `intake_enabled` is false (notice only; no form).
   - Home dentistry still requires Tehran area before advance.
   - Consent still loaded from `/api/v1/policies/case_coordination`; submit still uses idempotent draft + content_hash consent path.
   - Optional `priority` (`normal`|`urgent`) accepted on case draft API; defaults to `normal`.
   - Persian-first step copy in `fa`/`en`/`ar` `request.php`; brand progress bar uses `#2947A3` / `#162B70` / `#F7F5EF`.
   - Documents step is informational only — OPG remains quarantine → ClamAV; no diagnosis claims.

2. **CMS Orientation 7 fix (review P2)**
   - `CmsMediaSanitizer` EXIF Orientation 7 now rotates +90 then horizontal flip (transverse), not the Orientation 5 transpose path.

CI note: GitHub Actions jobs on this repo still complete in ~1s with failure before useful steps (same on `main` history). Treated as host/Actions policy; product changes above are independent.

Next hour priority:

1. Task-oriented six role dashboards + SLA timers.
2. Session inventory/revoke UI.
3. Homepage services search (`q`) usefulness if still needed.
4. Revisit Actions runner policy when an operator can inspect org settings.

Non-negotiables still held: Laravel 13, OTP/MFA, ClinicalDocumentPolicy, OPG quarantine, owner-has-no-clinical-access, no diagnosis, no payments, staging banner.


## 2026-09-16 20:00 EDT — hour 2

Automation: hourly scheduler `01a0ac98-4595-7253-b942-c68c1ecbf281` and durable Automations task `2728589c-3846-4539-8d00-496ef4fe62d2` (every 60 minutes, America/New_York, app-only). Never push `main`.

Shipped on this hour:

1. **CMS sanitizer review P1s**
   - Raster headers (`getimagesize`) are checked against `max_edge` / `max_pixels` *before* GD decode.
   - JPEG EXIF Orientation 2–8 is applied before re-encode.
   - API media index returns `route('cms.media.serve')` instead of a disk adapter URL.
   - Upload `max:` validation follows `royadarman.cms.media.max_bytes`.
2. **Blade homepage is service-first**
   - H1 is `چه خدمتی نیاز دارید؟` / `What do you need?` / `ما الخدمة التي تحتاجها؟`
   - Neighborhood search + Tehran coverage chips.
   - Staging banner `محیط آزمایشی` on non-production public chrome.
3. **Permanent `/pres/` portal**
   - Visible outside production; 404 in production unless `PANEL_DEMO_ACCESS`.
   - Role handoffs minted as 30-minute signed `/__panel-test/{role}` URLs only when demo access is on.
   - Reseed is owner/tech-admin only. Anonymous visitors cannot reset.

Next hour (priority):

1. Guided 9-step patient request in Blade (intake remains fail-closed).
2. Task-oriented six role dashboards + SLA timers.
3. Session inventory/revoke.
4. Investigate GitHub Actions: jobs on this repo currently die in 2–17s with empty steps (including `main`). Treat as infra, not a product regression, until a green run exists.

Non-negotiables still held: Laravel 13, OTP/MFA, ClinicalDocumentPolicy, OPG quarantine, no diagnosis, no payments.

## 2026-09-16 19:40 EDT — hour 1

Opened draft PR #9 from `presentation-rescue-2026-09-16`:

- Gap table
- `backend/package-lock.json` + CI frontend `npm ci && npm run build`
- `CmsMediaSanitizer` (reject SVG/GIF, sniff bytes, re-encode)

Persian staging preview of the product journey lives in the App Builder workspace; Laravel remains canonical.
