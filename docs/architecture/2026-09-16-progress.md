# Presentation rescue progress

Living log for branch `presentation-rescue-2026-09-16` / draft PR https://github.com/maziyarid/Royadarman/pull/9.

Hourly automation continues this file. Newest entry first.

## 2026-09-18 04:55 EDT — hour 13 (RPH-8 queue-timing contract)

Automation continues on `presentation-rescue-2026-09-16`. Never push `main`. Agiflow project `medical-websites-operations-growth`. Lane is RPH-4..RPH-10 only (not RPH-11/RPH-12).

Routing: PHP/Composer not on PATH — no PHPUnit invented. [CHATGPT REVIEW — b068639] confirmed Greptile P1 is VALID: static-guard still compared `--timeout` to a hard-coded 90, so a live `DB_QUEUE_RETRY_AFTER=85` override would pass. Continue RPH-8.

Shipped this hour:

1. **`infra/queue-timing.conf` is the non-secret source of truth**
   - `WORKER_TIMEOUT=85`, `DB_QUEUE_RETRY_AFTER=90`, `SAFETY_MARGIN=5`.
   - `infra/checks/static-guard.sh` parses those integers (no magic 90).
   - Unit `--timeout` must equal declared `WORKER_TIMEOUT`.
   - Declared retry_after − worker timeout must be ≥ `SAFETY_MARGIN`.
   - `host.env.example` and `backend/.env.example` lockstep with the contract.
2. **Fail-closed preflight on the live Laravel value**
   - `config('royadarman.queue.worker_timeout_seconds')` from `ROYADARMAN_QUEUE_WORKER_TIMEOUT` (default 85).
   - `royadarman:preflight` fails when `config('queue.connections.database.retry_after')` − worker timeout < margin.
   - Does not inspect systemd. `PreflightTest` covers 85/85 fail (`DB_QUEUE_RETRY_AFTER` in output) and 90/85 pass.
   - Tests written, not executed — PHP/Composer unavailable.
3. **P2 deploy/pint**
   - `infra/deploy.md`: `INTAKE_ENABLED=false`; preflight **before** `migrate --force`; re-preflight after; confirm live `retry_after` before `systemctl restart`.
   - `backend/DEPLOYMENT.md` and `infra/checks/clean-checkout.md` use `"$PHP" vendor/bin/pint --test`.
4. **static-guard re-run** — see commit / handoff for the real PASSED output.

Did not: systemctl, crontab, ClamAV, migrations, `main` push, PR #9 merge, RPH-11/RPH-12, reopen settled RPH-5 session logic.

Non-negotiables still held: Laravel 13, OTP/MFA, ClinicalDocumentPolicy, OPG quarantine, owner-has-no-clinical-access, no diagnosis, no payments, staging banner.

## 2026-09-17 23:15 EDT — hour 12 (RPH-8 static-guard numeric margin)

Automation continues on `presentation-rescue-2026-09-16`. Never push `main`. Agiflow project `medical-websites-operations-growth`. Lane is RPH-4..RPH-10 only (not RPH-11/RPH-12).

Routing: PHP 8.3.6 CLI present; Composer/vendor still absent — no PHPUnit invented. No new [CHATGPT REVIEW] of infra/. Greptile still flagged presence-only timeout checks. Continue RPH-8: parse worker timeout and enforce documented margin.

Shipped this hour:

1. **static-guard parses and compares timeout margin**
   - `infra/checks/static-guard.sh` extracts `--timeout=N` from the unit, requires the unit to document Laravel `DB_QUEUE_RETRY_AFTER` default 90, and fails unless margin ≥ 5s (90 − timeout).
   - Addresses Greptile P1: presence of tokens alone allowed equal timeout/retry values.

Did not: systemctl, crontab, ClamAV, migrations, `main` push, PR #9 merge, RPH-11/RPH-12, reopen settled RPH-5 session logic.

Non-negotiables still held: Laravel 13, OTP/MFA, ClinicalDocumentPolicy, OPG quarantine, owner-has-no-clinical-access, no diagnosis, no payments, staging banner.

## 2026-09-17 20:20 EDT — hour 11 (RPH-8 static-guard timeout contract)

Automation continues on `presentation-rescue-2026-09-16`. Never push `main`. Agiflow project `medical-websites-operations-growth`. Lane is RPH-4..RPH-10 only (not RPH-11/RPH-12).

Routing: PHP 8.3.6 CLI present; Composer/vendor still absent — no PHPUnit invented. RPH-9 Actions run 35275301812 on HEAD `2b8307d` still ~2s empty-step failures (same host/Actions pattern, not new evidence). No new [CHATGPT REVIEW] of infra/ yet. Continue RPH-8: lock the queue timeout contract into static-guard.

Shipped this hour:

1. **static-guard asserts worker timeout boundary**
   - `infra/checks/static-guard.sh` now requires `--timeout=85` and the `DB_QUEUE_RETRY_AFTER` documentation string in `systemd/royadarman-queue.service`.
   - Re-ran `sh infra/checks/static-guard.sh` → PASSED (no PHP).

Did not: systemctl, crontab, ClamAV, migrations, `main` push, PR #9 merge, RPH-11/RPH-12, reopen settled RPH-5 session logic.

Non-negotiables still held: Laravel 13, OTP/MFA, ClinicalDocumentPolicy, OPG quarantine, owner-has-no-clinical-access, no diagnosis, no payments, staging banner.

## 2026-09-17 17:10 EDT — hour 10 (RPH-8 queue timeout)

Automation continues on `presentation-rescue-2026-09-16`. Never push `main`. Agiflow project `medical-websites-operations-growth`. Lane is RPH-4..RPH-10 only (not RPH-11/RPH-12).

Routing: PHP CLI present (8.3.6) but Composer/vendor still unavailable — no PHPUnit invented. RPH-9 has no new Actions evidence. Continue RPH-8 after Greptile P1 on HEAD `2efa8eb`.

Shipped this hour:

1. **Queue worker timeout below reservation retry**
   - `infra/systemd/royadarman-queue.service` `--timeout=85` (was 90).
   - Documents that worker timeout must sit several seconds below Laravel `DB_QUEUE_RETRY_AFTER` (default 90s) so a long job cannot be re-claimed before the original process exits.
   - Placeholders only; committing does not install or restart the unit.

Did not: systemctl, crontab, ClamAV, migrations, `main` push, PR #9 merge, RPH-11/RPH-12, reopen settled RPH-5 session logic.

Non-negotiables still held: Laravel 13, OTP/MFA, ClinicalDocumentPolicy, OPG quarantine, owner-has-no-clinical-access, no diagnosis, no payments, staging banner.

## 2026-09-17 16:40 EDT — hour 9 (RPH-8 infra templates)

Automation continues on `presentation-rescue-2026-09-16`. Never push `main`. Agiflow project `medical-websites-operations-growth`. Lane is RPH-4..RPH-10 only (not RPH-11/RPH-12).

Routing this hour: [CHATGPT REVIEW — 740cdcc] found no new RPH-5 static blocker. PHP/Composer still unavailable (no invented PHPUnit). RPH-9 run 35251512142 on HEAD `740cdcc` still ~2s empty-step failure (same host/Actions pattern). RPH-8 was the groomed fallback (Todo → In Progress). Did not reopen settled session logic.

Shipped this hour:

1. **`infra/` source-of-truth templates** (placeholders only; do not apply by commit)
   - `systemd/royadarman-queue.service` — `ea-php83` worker on `otp,scanning,notifications,maintenance`.
   - `cron/royadarman` — minute `schedule:run` via `ea-php83`.
   - `clamav.md` — fail-closed `/usr/bin/clamscan` expectations (EICAR is operator-window only).
   - `host.env.example` — documented AlmaLinux/cPanel paths; no secrets.
   - `deploy.md` — additive migrate, release-identity, rollback-keep-schema.
2. **Validation procedure**
   - `infra/checks/static-guard.sh` (no PHP) — passed this hour.
   - `infra/checks/clean-checkout.md` documents PHPUnit/preflight (not executed) vs live smoke (not executed).
3. `backend/DEPLOYMENT.md` points at `infra/` and restates that git is not a host apply.

Did not: systemctl, crontab, ClamAV, migrations, `main` push, PR #9 merge, RPH-11/RPH-12.

Non-negotiables still held: Laravel 13, OTP/MFA, ClinicalDocumentPolicy, OPG quarantine, owner-has-no-clinical-access, no diagnosis, no payments, staging banner.

## 2026-09-17 13:20 EDT — hour 8 (RPH-5 reactivation revoke + resolver)

Automation continues on `presentation-rescue-2026-09-16`. Never push `main`. Agiflow project `medical-websites-operations-growth`. Lane is RPH-4..RPH-10 only (not RPH-11/RPH-12).

Addresses [CHATGPT REVIEW — d9cd6c3]. Greptile 5/5 on `d9cd6c3` remains review evidence only — not a merge decision.

Shipped this hour:

1. **Inactive→active staff reactivation is a sensitive identity transition**
   - `ProvisionStaff` now includes `$reactivated = ! $user->is_active` in `$shouldRevoke`.
   - Audit reason `staff_reactivated` when the only sensitive change is reactivation.
   - Role/MFA reasons still take precedence (`staff_role_change_and_mfa`, `staff_role_change`, `staff_mfa_replaced`).
   - Same revoke-first + identity persist transaction as hour 7.
   - Closes the dormant-session hole: `EnsureActiveUser` only invalidates a request that actually presents an inactive session.
2. **Preflight uses `SessionInventoryService` connection resolver**
   - `sharesAuditConnection()` / `resolvedSessionConnectionName()` / `resolvedAuditConnectionName()` are public.
   - `royadarman:preflight` no longer hardcodes AuditEvent = `database.default`.
3. **Tests (written, not executed — PHP CLI/Composer unavailable here)**
   - `StaffProvisioningTest::test_reactivating_inactive_staff_revokes_dormant_sessions`.
   - `SessionInventoryTest` covers `staff_reactivated` plus the public resolver contract.

Do not merge PR #9. RPH-5 stays In Progress pending PHPUnit.

Non-negotiables still held: Laravel 13, OTP/MFA, ClinicalDocumentPolicy, OPG quarantine, owner-has-no-clinical-access, no diagnosis, no payments, staging banner.

## 2026-09-17 10:15 EDT — hour 7 (RPH-5 identity+revoke atomicity)

Automation continues on `presentation-rescue-2026-09-16`. Never push `main`. Agiflow project `medical-websites-operations-growth`. Lane is RPH-4..RPH-10 only (not RPH-11/RPH-12).

Shipped this hour:

1. **Greptile P1 `discussion_r4037591611`**
   - `ProvisionStaff` no longer commits role/MFA and then calls `revokeAll`.
   - Existing-identity updates wrap **revoke first, then identity persist** in one user/audit-connection transaction.
   - Same-connection audit failure cannot restore sessions on an already-elevated/rotated identity.
   - New identities still skip revoke.
2. **Split-store production decision (ChatGPT residual, option a)**
   - `royadarman:preflight` **fails in production** if `SESSION_CONNECTION` is split from the AuditEvent/default connection.
   - Non-production warns only. No fake XA. No outbox in this slice.
3. **Tests (written, not executed — PHP CLI/Composer unavailable here)**
   - `StaffProvisioningTest`: force-role-change revokes sessions; same-connection audit failure leaves old role + session.
   - `PreflightTest`: production split SESSION_CONNECTION fails; local split still succeeds.
   - Encrypted `AuditEvent.reason` assertions no longer use SQL `where('reason')`.

CI note: Actions run 35228826827 on HEAD `164c175` still completes in ~1s with **empty job steps** and log blob 404. Host/Actions policy (RPH-9), not an application-test result.

Do not merge PR #9. RPH-5 stays In Progress pending PHPUnit.

Non-negotiables still held: Laravel 13, OTP/MFA, ClinicalDocumentPolicy, OPG quarantine, owner-has-no-clinical-access, no diagnosis, no payments, staging banner.

## 2026-09-17 09:40 EDT — hour 6 (RPH-5 AC3)


Automation continues on `presentation-rescue-2026-09-16`. Never push `main`. Agiflow task RPH-5 (Medical Websites board).

Shipped this hour:

1. **AC3 — revoke sessions on sensitive identity/security changes**
   - `royadarman:staff:provision` now injects `SessionInventoryService`.
   - Role change on an existing identity (`--force-role-change` path) calls `revokeAll(..., 'staff_role_change')`.
   - TOTP/recovery replacement (`--replace-mfa` or first-time MFA on existing user) calls `revokeAll` with `staff_mfa_replaced` or `staff_role_change_and_mfa` as appropriate.
   - New identities skip revoke (no sessions).
2. **Tests**
   - `SessionInventoryTest::test_security_change_reasons_are_audited_on_revoke_all` covers the audit reasons ProvisionStaff uses.
   - PHPUnit still not executed here: PHP 8.3 CLI is present; Composer/vendor are not.

Not done: executable PHPUnit evidence; split-store audit policy/preflight (ChatGPT residual); RPH-4 MariaDB/VPS; RPH-9 CI empty-step. Do not merge PR #9.

Next hour priority:

1. Independent PHPUnit of `SessionInventoryTest` when Composer is available.
2. Optional preflight note that production should prefer same-connection session+audit.
3. RPH-6 referral SLA append-only events only after RPH-5 evidence.

Non-negotiables still held: Laravel 13, OTP/MFA, ClinicalDocumentPolicy, OPG quarantine, owner-has-no-clinical-access, no diagnosis, no payments, staging banner.

## 2026-09-17 07:05 EDT — hour 5 (RPH-5 session P1s)

Automation continues on `presentation-rescue-2026-09-16`. Never push `main`. Agiflow task RPH-5.

Shipped this hour:

1. **Session inventory uses the configured session store (Greptile P1 `discussion_r4035847105`)**
   - `SessionInventoryService` reads `config('session.connection')` and `config('session.table')`.
   - Null `SESSION_CONNECTION` still means the default DB connection.
2. **Revoke + audit consistency (Greptile P1 `discussion_r4035847117`)**
   - Same connection as `AuditEvent`: delete + encrypted audit insert share one transaction (including both `session.revoke_all` and `session.force_revoke_all`).
   - Split store: no fake XA. Revoke first, best-effort audit (`report()`, do not rethrow). HTTP still reports success after a successful delete.
3. **Tests added (not executed in this shared environment — PHP/Composer unavailable)**
   - Split sqlite `:memory:` connection: inventory ignores default-connection decoy rows; revoke hits only the session store.
   - Same-connection `AuditEvent::creating` throw rolls the session delete back.
   - Split-connection audit throw still deletes the session row and does not bubble.
4. **Runbook** documents the connection + consistency model.

Review classification vs HEAD `30917721` (pre-this-commit):

| Thread | Class |
|---|---|
| `discussion_r4035847105` session connection | **valid** — fixed this hour |
| `discussion_r4035847117` revoke/audit atomicity | **valid** — fixed this hour with documented split-store semantics |
| `discussion_r4032845723` Arabic wizard | **already fixed** — `fa`/`ar`/`en` `request.php` each have 70 lockstep keys |
| `discussion_r4031843946` homepage search | **already fixed** — `PublicPageController::services` ranks `?q=` |

Not in this slice: RPH-5 AC3 (revoke on phone/TOTP/role change). RPH-4 still blocked (no MariaDB/VPS). PHPUnit not run here. GitHub Actions still empty-step red (RPH-9). Do not merge PR #9.

Next hour priority:

1. Independent PHPUnit of `SessionInventoryTest` when a PHP 8.3 runner exists.
2. RPH-5 AC3 — revoke other sessions on sensitive identity changes.
3. RPH-6 referral SLA append-only events.

Non-negotiables still held: Laravel 13, OTP/MFA, ClinicalDocumentPolicy, OPG quarantine, owner-has-no-clinical-access, no diagnosis, no payments, staging banner.

## 2026-09-16 23:15 EDT — hour 3 (truncated for length in prior hours)

See repository history for hours 1–4 content.
